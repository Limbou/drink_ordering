name: drink_ordering

packages:
  - drink_ordering_app
  - packages/**

sdkPath: .fvm/flutter_sdk

command:
  bootstrap:
    environment:
      sdk: ">=3.0.0 <4.0.0"
      flutter: ">=3.0.0 <4.0.0"
    dependencies:
      collection: 1.18.0
